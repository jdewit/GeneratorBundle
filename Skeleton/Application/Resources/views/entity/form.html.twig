{% for field in fields %}
{% if field.type == 'oneToMany' or field.type == 'manyToMany' %}
{{ "{{ form_row({{ entity_lc }}form."~ field.fieldName ~"s) }}" }}    
{% else %}
{{ "{{ form_row({{ entity_lc }}form."~ field.fieldName ~") }}" }}    
{% endif %}
{% endfor %} 
{{ "{{ form_widget({{ entity_lc }}form._token) }}" }}
{{ "{{ form_rest({{ entity_lc }}form) }}" }}
