{% for field in fields %}
{% if field.type == 'oneToMany' or field.type == 'manyToMany' %}
{{ "{{ form_row(form."~ field.fieldName ~"s) }}" }}    
{% else %}
{{ "{{ form_row(form."~ field.fieldName ~") }}" }}    
{% endif %}
{% endfor %} 
{{ "{{ form_rest(form) }}" }}
