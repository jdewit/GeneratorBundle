    /**
     * Delete one {{ entity }}.
     *
     * @Route("/delete/{id}", name="{{ bundleAlias }}_{{ entityCC }}_delete")
     */
    public function deleteAction($id)
    {
        ${{ entityCC }} = $this->container->get('{{ bundleAlias }}.{{ entityCC }}_manager')->find($id);
        ${{ entityCC }}Manager = $this->container->get('{{ bundleAlias }}.{{ entityCC }}_manager');
        
        if (${{ entityCC }}Manager->delete(${{ entityCC }})) {

            $this->container->get('session')->getFlashBag()->set('success', '{{ entityTitle }} deleted.');
        } else {
            $this->container->get('session')->getFlashBag()->set('error', 'Unable to delete {{ entityTitle }}.');
        }     

        return new RedirectResponse($this->container->get('router')->generate('{{ bundleAlias }}_{{ entityCC }}_list'), 301);     
    }

