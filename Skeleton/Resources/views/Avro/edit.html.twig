{{ "{% extends '"~ bundleName ~"::layout.html.twig' %}" }}
{{ "{% block content %}" }}
    <div class="page-header">
        <h1>Edit {{ entityTitle }}</h1>
    </div>
    <form id="{{ bundleAlias }}_{{ entityCC }}_form" class="form-horizontal" method="post" action="{{ "{{ path('"~ bundleAlias ~"_"~ entityCC ~"_edit', { 'id': "~ entityCC ~".id }) }}" }}" {{ "{{ form_enctype(form) }}" }}>  
        {{ "{% include '"~ bundleName ~":"~ entity ~":form.html.twig' %}" }}
        <div class="form-actions">
            <button class="btn btn-primary" title="Update the {{ entityTitle }}"><i class="sprite-tick"></i> Update {{ entityTitle }}</button>
            <a class="btn" href="{{ "{{ path('"~ bundleAlias ~"_"~ entityCC ~"_list') }}" }}" title="Go back to {{ entityTitle }} list.">Back to list</a>
            <a class="btn confirm btn-danger pull-right" href="{{ "{{ path('"~ bundleAlias ~"_"~ entityCC ~"_delete', { 'id': "~ entityCC ~".id }) }}" }}" title="Are you sure you want to delete this {{ entityTitleLC }}?">Delete</a>
        </div>
    </form>
{{ "{% endblock %}" }}
{% block customJS %}
    ;(function(avro, $, undefined) {
        avro.{{ entityCC }}Model = new {{ entityCC }}Model();
{% for field in uniqueManyToOneRelations %}
        avro.available{{ entity }}s = {{ "{{ app.request.attributes.get('"~ entityCC ~"s') | serialize | raw }}" }};
{% endfor %}
    }(window.avro = window.avro || {}, jQuery)); 

    ko.applyBindings(avro.{{ entityCC }}Model);
{% endblock %}
