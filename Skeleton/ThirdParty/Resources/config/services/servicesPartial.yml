#{{ entity_cc }}      
    {{ bundle_alias }}.{{ entity_us }}_manager':
        class: {{ bundle_namespace }}\Entity\{{ entity }}Manager
        arguments: ["@doctrine.orm.entity_manager", {{ bundle_namespace }}\Entity\{{ entity }}, "@security.context"]
        
    {{ bundle_alias }}.{{ entity_us }}.form.type:
        class: {{ bundle_namespace }}\Form\Type\{{ entity }}FormType
        tags: 
            - { name: form.type, alias: {{ bundle_alias }}_{{ entity_us }} }
        arguments: ["@security.context"]
       
    {{ bundle_alias }}.{{ entity_us }}.form:
        factory_method: createNamed 
        factory_service: form.factory
        class: Symfony\Component\Form\Form
        arguments: ["@{{ bundle_alias }}.{{ entity_us }}.form.type", {{ bundle_alias }}_{{ entity_us }}]
        
    {{ bundle_alias }}.{{ entity_us }}.form.handler:
        class: {{ bundle_namespace }}\Form\Handler\{{ entity }}FormHandler
        scope: request
        arguments: ["@{{ bundle_alias }}.{{ entity_us }}.form", "@request", "@{{ bundle_alias }}.{{ entity_us }}_manager'"]
