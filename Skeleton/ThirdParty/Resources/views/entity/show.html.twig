{{ "{% extends '"~ bundle_name ~"::layout.html.twig' %}" }}
{{ "{% block content %}" }}
    <div class="header ui-state-active">
        <h1>{{ entity }}</h1>
    </div>
    <div class="content">  
{% for field in fields %}
        <div>{{ field.fieldName|camelCaseToTitle }}: {{ "{{ "~ entity_cc ~"."~ field.fieldName ~" }}" }}</div>
{% endfor %}
    </div>            
{{ "{% endblock %}" }}

