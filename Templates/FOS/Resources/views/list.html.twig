{{ "{% extends '"~ bundleName ~"::layout.html.twig' %}" }}
{{ "{% block content %}" }}
    <div class="page-header">
        <h1>
            {{ entity }} List
        
            <a href="{{ "{{ path('"~ bundleAlias ~"_"~ entityCC ~"_new') }}" }}" class="btn btn-primary btn-large pull-right">+ New {{ entity }}</a>
        </h1>
    </div>
    <div class="row">
        {{ "{% include '"~ bundleName~":"~ entity ~":table.html.twig' %}" }}
    </div>
{{ "{% endblock %}" }}

